% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{guess_column}
\alias{guess_column}
\title{Guess a column for a data-mapping role}
\usage{
guess_column(cols, kind = c("text", "label", "id"), exclude = character())
}
\arguments{
\item{cols}{A character vector of column names.}

\item{kind}{The mapping role to identify: \code{"text"}, \code{"label"}, or \code{"id"}.}

\item{exclude}{Column names that are already assigned to another role.}
}
\value{
One column name as a character vector of length one, or \code{NULL} when
no column remains.
}
\description{
Column names are matched without regard to case or common separators.
When no recognized name remains, the first available column is returned.
Text names include \code{text}, \code{body}, \code{content}, \code{response}, \code{message}, and
\code{utterance}; label names include \code{label}, \code{labels}, \code{code}, \code{category}, and
\code{class}; identifier names include \code{id}, \code{doc_id}, and \code{unit_id}.
}
\examples{
cols <- c("id", "text", "label")
text_col <- guess_column(cols, "text")
guess_column(cols, "label", exclude = text_col)
}
