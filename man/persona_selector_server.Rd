% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persona_selector.R
\name{persona_selector_server}
\alias{persona_selector_server}
\title{Server for the persona selector module}
\usage{
persona_selector_server(
  id,
  data,
  overview = NULL,
  page_length = 8L,
  height = "260px"
)
}
\arguments{
\item{id}{Module id.}

\item{data}{A persona data frame (or a \code{reactive} returning one).}

\item{overview}{Optional overview data frame, or a function \verb{function(df)}
building one. Defaults to \code{\link[LLMR:llm_persona_overview]{LLMR::llm_persona_overview()}} when LLMR is
available, else the first columns of \code{data}.}

\item{page_length}{Rows per page in the table. Default \code{8}.}

\item{height}{CSS height for the table body. Default \code{"260px"}.}
}
\value{
A \code{reactive} returning an integer vector of selected row indices into
\code{data} (\code{integer(0)} when nothing is selected).
}
\description{
Renders a compact overview of \code{data} as a multi-select table and returns the
selected row indices (relative to \code{data}) as a reactive. When \code{data} carries
the persona contract (see \code{\link[LLMR:llm_persona_overview]{LLMR::llm_persona_overview()}}), the overview columns
are chosen automatically; otherwise the first few columns are shown.
}
\seealso{
\code{\link[=persona_selector_ui]{persona_selector_ui()}}.
}
